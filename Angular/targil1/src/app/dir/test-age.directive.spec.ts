import { TestAgeDirective } from './test-age.directive';

describe('TestAgeDirective', () => {
  it('should create an instance', () => {
    const directive = new TestAgeDirective();
    expect(directive).toBeTruthy();
  });
});
